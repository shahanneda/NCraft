{
    "version": "2.0.0",
    "tasks": [{
        "label": "buildProgram",
        "command": "cmake --build build", // Could be any other shell command
        // "args": ["test"],
        "type": "shell"
    }]
}